# Required in production (console.anthropic.com → API Keys)
ANTHROPIC_API_KEY=sk-ant-...

# Password protection for the panel — required in production.
# If left empty, the panel runs with NO password (fine for local use only).
HUB_USERNAME=vlouxe
HUB_PASSWORD=change-me

PORT=4321
